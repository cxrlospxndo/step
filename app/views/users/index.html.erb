
<ul data-role="listview">
  <% @users.each do |user| %>
      <li><img src=<%=user.pic%> ><h3><%= link_to user.fullname, user %></h3><p><%=user.codigo%></p></li>
  <% end %>
</ul>
<br />

<div data-role="navbar">
  <ul>
    <li><%= link_to 'New', new_user_path, 'data-role' => 'button', 'data-icon' => 'plus', 'data-theme' => 'b' %></li>
  </ul>
</div>
